<%= link_to 'sign_out', destroy_user_session_path, method: :delete %>
<% @docs.each do |doc| %>
<h2>
  <%= doc.title %>
</h2>

<p>
  <%= time_ago_in_words(doc.created_at) %>
</p>

<p>
  <%= truncate(doc.content, length:150) %>
</p>

<% end %>

<%= link_to 'Create Doc', docs_new_path %>
