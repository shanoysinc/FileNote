<div class="container containerize">
<%= simple_form_for(@doc, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.button :submit %>
<% end %>
</div>
